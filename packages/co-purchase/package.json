{
  "name": "@realriches/co-purchase",
  "version": "1.0.0",
  "description": "Non-custodial co-purchase group workspace for collaboration, verification, and document collection",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@realriches/database": "workspace:*",
    "@realriches/utils": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "peerDependencies": {
    "@realriches/document-storage": "workspace:*"
  }
}

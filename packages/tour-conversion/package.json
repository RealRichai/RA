{
  "name": "@realriches/tour-conversion",
  "version": "1.0.0",
  "description": "3DGS Tour conversion service with PLY->SOG pipeline, QA, and provenance tracking",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "worker": "tsx src/worker.ts"
  },
  "dependencies": {
    "@playcanvas/splat-transform": "^0.16.1",
    "bullmq": "^5.1.0",
    "ioredis": "^5.3.2",
    "sharp": "^0.33.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "@realriches/database": "workspace:*",
    "@realriches/feature-flags": "workspace:*"
  }
}
